name: balance_service_test

on:
  push

jobs:
  tests:
    runs-on:
      group: runners_v1
    env:
      LLVL: trace
    steps:
      - name: Use Go = 1.23
        uses: actions/setup-go@v3
        with:
          go-version: '=1.23'
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v3

      # Install gotestfmt on the VM running the action.
      - name: Set up gotestfmt
        uses: gotesttools/gotestfmt-action@v2
        with:
          # Pass GITHUB_TOKEN to avoid rate limiting.
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create temp directory
        run: |
          mkdir -p ./${{ github.run_attempt }}

      # Run Balance Service unit tests with nice formatting
      - name: Run Balance Service unit tests
        run: |
          set -euo pipefail
          go test -json -timeout 5m -v -race -run TestTokenBalanceManager ./peer/tests/unit 2>&1 | tee ./${{ github.run_attempt }}/gotest-balanceService-unit.log | gotestfmt

      # Upload formatted logs
      - name: Upload Balance Service unit test log
        uses: actions/upload-artifact@v4
        with:
          name: balance-service-unit-test-log
          path: ./${{ github.run_attempt }}/gotest-balanceService-unit.log
          if-no-files-found: error
